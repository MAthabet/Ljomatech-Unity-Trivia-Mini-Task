//not used for now as it is not in the task requirements
/*
using UnityEngine;
using UnityEngine.UI;


/// <summary>
/// component to handle next question button events
/// </summary>
[RequireComponent(typeof(Button))]
public class NextQuestionButton : MonoBehaviour
{
    private Button button;

    void Awake()
    {
        button = GetComponent<Button>();

        button.onClick.AddListener(OnNextQuestionClicked);
    }

    private void OnNextQuestionClicked()
    {
        GameEvents.NextQuestion();
    }
}
*/